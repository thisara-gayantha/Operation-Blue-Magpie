# S1 — The Phishing Lure  (Digital Forensics · Easy)

**Container:** none — static file attachment in CTFd.
**Owner:** Challenge Design A (S1–S3).

## Artefact
- `lure.eml` — a phishing email. Analysis (headers / body / base64 / strings)
  reveals the flag and the hand-off values.

## Hand-off to S2 (locked)
- Staging URL: `http://staging.ceylonpay.lk`
- Filename to find next: `network_diagram.png`

## Flag
- Format `CPAY{S1_<slug>_<6-hex-token>}` (final token generated by
  `scripts/gen_flags.py`, kept out of git history where practical).

## Tools (intended)
text editor · `strings` · `base64` · `exiftool`

Generator script + final `lure.eml` land in Step 5.
